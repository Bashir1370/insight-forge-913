# Scientific review — RNA-seq lesson 9

درس ۹ «نرمال‌سازی و بیان افتراقی» با تمرکز بر آموزش مبتدی و با تکیه بر منابع اولیه DESeq2 بازبینی شده است.

## منابع اصلی

- DESeq2 vignette — *Analyzing RNA-seq data with DESeq2*: https://bioconductor.org/packages/release/bioc/vignettes/DESeq2/inst/doc/DESeq2.html
- DESeq2 source/man pages: https://github.com/thelovelab/DESeq2
- Love MI, Huber W, Anders S. 2014. *Moderated estimation of fold change and dispersion for RNA-seq data with DESeq2*. Genome Biology 15:550.

## نکات علمی تثبیت‌شده در درس

1. **ورودی مدل شمارشی**: DESeq2 شمارش‌های خام/برآوردی مناسب را می‌گیرد و تصحیح مقیاس کتابخانه را در مدل انجام می‌دهد؛ VST/rlog یا TPM به‌عنوان ورودی آزمون استاندارد DESeq2 آموزش داده نمی‌شوند.
2. **ضریب مقیاس نمونه**: منطق استاندارد DESeq2 برآورد size factor با روش median-ratio است. درس این مفهوم را بدون فرمول سنگین معرفی می‌کند و فرض وجود مجموعه بزرگی از ژن‌های مرجع نسبی را گوشزد می‌کند.
3. **سوگیری ترکیب کتابخانه**: مجموع کل شمارش همیشه معیار بی‌طرفی برای مقیاس نیست؛ تغییر شدید در تعداد محدودی ژن می‌تواند سهم نسبی سایر ژن‌ها را جابه‌جا کند.
4. **تکرار زیستی و پراکندگی**: مدل دوجمله‌ای منفی DESeq2 میانگین و پراکندگی ژن‌ویژه را در نظر می‌گیرد. تحلیل DESeq2 بدون تکرار زیستی پشتیبانی نمی‌شود.
5. **طراحی چندعاملی**: فرمول‌هایی مانند `~ batch + condition` فقط وقتی معنا دارند که عوامل از هم قابل تفکیک باشند. درهم‌آمیختگی کامل batch و condition با افزودن متغیر به مدل حل نمی‌شود.
6. **اندازه اثر**: log2 fold change به‌عنوان اندازه و جهت اثر آموزش داده می‌شود؛ `+1` تقریباً دو برابر و `-1` تقریباً نصف است.
7. **مقدار p**: به‌عنوان احتمال مشاهده آماره‌ای به این شدت یا شدیدتر تحت فرض صفر تعریف می‌شود، نه احتمال درست‌بودن فرض صفر.
8. **آزمون‌های متعدد**: padj و FDR در سطح مجموعه آزمون‌ها توضیح داده می‌شوند. padj نباید به‌عنوان «احتمال کاذب‌بودن همین ژن» معرفی شود.
9. **معنی‌داری آماری و اهمیت زیستی**: اندازه اثر، عدم‌قطعیت و زمینه زیستی باید کنار هم دیده شوند؛ هیچ آستانه اثر واحدی برای همه پروژه‌ها معرفی نشده است.
10. **ابزار بعد از مفهوم**: نام DESeq2 و توابع `DESeq()`/`results()` تنها پس از ساختن مفاهیم نرمال‌سازی، پراکندگی، طراحی، اثر و آزمون وارد می‌شوند.

## نکته آموزشی

درس عمداً وارد جزئیات پیشرفته‌ای مثل LFC shrinkage، independent filtering، Cook's distance و آزمون LRT نمی‌شود. این مفاهیم می‌توانند در لایه پیشرفته یا پروژه یکپارچه اضافه شوند تا مسیر پایه برای دانشجوی تازه‌کار بیش از حد سنگین نشود.
