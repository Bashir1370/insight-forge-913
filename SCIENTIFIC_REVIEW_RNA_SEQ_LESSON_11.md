# بازبینی علمی درس ۱۱ RNA-seq — پروژه یکپارچه سرطان پانکراس

این درس یک **سناریوی آموزشی شبیه‌سازی‌شده** است. شناسه نمونه‌ها، درصدهای QC، مقادیر بیان افتراقی و نتیجه‌های زیستی نمایش‌داده‌شده داده واقعی بیمار یا نتیجه یک مطالعه منتشرشده نیستند.

## اصول علمی تثبیت‌شده در سناریو

1. واحد مستقل زیستی در مثال بیمار است؛ فایل FASTQ، Read 1/Read 2 یا تکرار فنی جای تکرار زیستی را نمی‌گیرد.
2. گروه زیستی و دسته آزمایشی عمداً درهم‌آمیخته کامل نیستند تا اثر condition و batch در اصل قابل تفکیک باشند.
3. RIN به‌تنهایی به‌عنوان آستانه جهانی حذف استفاده نمی‌شود؛ تناسب نمونه با راهبرد کتابخانه و شواهد QC در زمینه پروتکل دیده می‌شود.
4. ۲۴ فایل FASTQ فقط در مثال ساده‌شده ۱۲ کتابخانه paired-end با یک جفت فایل به ازای هر نمونه است؛ تعداد فایل در پروژه واقعی می‌تواند تحت تأثیر lane، demultiplexing و قرارداد نام‌گذاری باشد.
5. هشدار آداپتور در C4 ابتدا با اقدام قابل اصلاح و QC مجدد دنبال می‌شود؛ حذف نمونه از یک هشدار منفرد نتیجه نمی‌شود.
6. مرجع ژنومی، حاشیه‌نویسی و منطق کمی‌سازی برای همه نمونه‌ها ثابت و نسخه‌دار نگه داشته می‌شود.
7. تطابق شناسه ستون‌های ماتریس با فراداده بخشی از کنترل تحلیل است، نه یک مرحله اداری فرعی.
8. خروج C4 فقط پس از همگرایی چند شاهد مستقل فنی در سناریو انجام می‌شود: نگاشت پایین، انتساب ژنی پایین، فاصله شدید در ساختار نمونه‌ها و یادداشت آزمایشگاهی. T6 فقط به دلیل RIN پایین‌تر یا استرومای بیشتر حذف نمی‌شود.
9. در بخش DESeq2، سناریو از شمارش مناسب برای مدل شمارشی استفاده می‌کند و design برابر `~ batch + condition` است. تبدیل‌های نمایشی مثل VST/rlog ورودی جایگزین مدل شمارشی نیستند.
10. خروجی بیان افتراقی با اندازه اثر، مقدار p و مقدار p تعدیل‌شده خوانده می‌شود؛ ادعای زیستی فقط از «ژن معنی‌دار» ساخته نمی‌شود.
11. تفسیر GSEA/مجموعه ژنی برای ساخت فرضیه استفاده می‌شود. در bulk RNA-seq افزایش یک تم ایمنی یا استرومایی به‌تنهایی منشأ سلولی یا علیت را اثبات نمی‌کند.
12. گزارش نهایی باید ورودی‌ها، نسخه‌ها، پارامترها، تصمیم‌های QC، نمونه‌های خارج‌شده و علت، مدل آماری، خروجی کامل و محدودیت‌ها را ثبت کند تا ردپای تحلیل قابل بازسازی باشد.

## منابع اولیه و رسمی استفاده‌شده برای بازبینی

- DESeq2 vignette, Bioconductor: https://bioconductor.org/packages/release/bioc/vignettes/DESeq2/inst/doc/DESeq2.html
- End-to-end RNA-Seq workflow, Bioconductor: https://www.bioconductor.org/help/course-materials/2015/CSAMA2015/lab/rnaseqCSAMA.html
- FastQC documentation — Adapter Content, Babraham Bioinformatics: https://www.bioinformatics.babraham.ac.uk/projects/fastqc/Help/3%20Analysis%20Modules/10%20Adapter%20Content.html
- GSEA User Guide, GSEA-MSigDB: https://docs.gsea-msigdb.org/GSEA/GSEA_User_Guide/

## نکته آموزشی

درس ۱۱ عمداً «مفهوم جدید» را محور قرار نمی‌دهد. هدف آن انتقال دانشجو از یادگیری قطعه‌ای به تصمیم‌گیری زنجیره‌ای است:

`سؤال ← طراحی ← نمونه/RNA ← کتابخانه ← FASTQ ← QC ← نگاشت/کمی‌سازی ← ماتریس/فراداده ← QC نمونه ← مدل آماری ← تفسیر ← گزارش قابل بازتولید`

هر عدد و تصمیم سناریو باید به‌عنوان مثال آموزشی دیده شود، نه آستانه یا نسخه استاندارد جهانی برای پروژه‌های RNA-seq.
