# بازبینی رفتار ذخیره پیشرفت آموزشی

این تغییر فقط رفتار نگهداری وضعیت رابط کاربری را اصلاح می‌کند و محتوای علمی درس‌ها را تغییر نمی‌دهد.

## هدف

در درس‌های مبتنی بر `GuidedConceptLesson`، پس از پاسخ‌دادن به ایستگاه‌های تصمیم و بازشدن بخش‌های بعد، Refresh صفحه نباید پاسخ‌ها و بخش‌های بازشده را دوباره قفل کند.

## رفتار جدید

- شناسه ذخیره برای هر درس از `sectionId` ساخته می‌شود؛ بنابراین پیشرفت درس‌ها با هم مخلوط نمی‌شود.
- `currentIndex`، پاسخ‌های ثبت‌شده و `maxUnlocked` در `localStorage` ذخیره می‌شوند.
- بازیابی فقط در مرورگر انجام می‌شود تا SSR و hydration به `window` وابسته نشوند.
- تا پیشرفت ذخیره‌شده خوانده نشده است، اثر ذخیره‌سازی اجازه ندارد وضعیت خالی اولیه را روی داده قبلی بنویسد.
- داده ذخیره‌شده قبل از استفاده از نظر نوع و محدوده اندیس‌ها پاک‌سازی می‌شود.
- خراب یا غیرفعال بودن `localStorage` نباید مانع استفاده از درس شود؛ در این حالت فقط persistence از دست می‌رود.

## محدوده

این لایه در کامپوننت مشترک `GuidedConceptLesson` اعمال می‌شود، بنابراین همه درس‌هایی که از این چارچوب استفاده می‌کنند رفتار یکسان خواهند داشت.

ذخیره فعلی دستگاه‌محور است. همگام‌سازی بین دستگاه‌ها/مرورگرها یا اتصال به حساب کاربری نیازمند لایه سروری جداگانه است و عمداً در این اصلاح کوچک وارد نشده است.
